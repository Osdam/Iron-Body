<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
>
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>
    <php>
        <env name="APP_ENV" value="testing"/>
        <env name="APP_MAINTENANCE_DRIVER" value="file"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="BROADCAST_CONNECTION" value="null"/>
        <env name="CACHE_STORE" value="array"/>
        <!-- TOOLING DE TEST ÚNICAMENTE (no producción). La suite usa SQLite
             en memoria efímera: rápida, aislada y sin tocar PostgreSQL. El
             código de producción/desarrollo corre SIEMPRE sobre PostgreSQL
             (ver .env → DB_CONNECTION=pgsql). El usuario de BD `ironbody` no
             tiene permiso CREATE DATABASE, por lo que no se provisiona una BD
             de test en Postgres; esta DB en memoria no es fuente de verdad. -->
        <env name="DB_CONNECTION" value="sqlite"/>
        <env name="DB_DATABASE" value=":memory:"/>
        <env name="DB_URL" value=""/>
        <env name="MAIL_MAILER" value="array"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <env name="SESSION_DRIVER" value="array"/>
        <!-- Baseline DETERMINISTA de integraciones externas: la suite NO depende
             del .env real (que ya trae credenciales LiveKit/Twilio). Los tests de
             proveedor activo fuerzan su propia config en tiempo de ejecución. -->
        <env name="LIVE_ENABLED" value="false"/>
        <env name="OTP_DRIVER" value="dev"/>
        <env name="PULSE_ENABLED" value="false"/>
        <env name="TELESCOPE_ENABLED" value="false"/>
        <env name="NIGHTWATCH_ENABLED" value="false"/>
    </php>
</phpunit>
